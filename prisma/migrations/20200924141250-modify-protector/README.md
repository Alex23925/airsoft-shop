# Migration `20200924141250-modify-protector`

This migration has been generated by Alex23925 at 9/24/2020, 7:12:50 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."Protector" DROP COLUMN "multiplier",
ADD COLUMN "effect" text   NOT NULL 
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200919043223-add-protector..20200924141250-modify-protector
--- datamodel.dml
+++ datamodel.dml
@@ -1,33 +1,17 @@
-// This is your Prisma schema file,
-// learn more about it in the docs: https://pris.ly/d/prisma-schema
-
-datasource db {
-  provider = "postgresql"
-  url = "***"
-}
-
 generator client {
   provider = "prisma-client-js"
 }
-model Protector {
-  id         Int      @id @default(autoincrement())
-  createdAt  DateTime @default(now())
-  img        String   @default("protector")
-  name       String
-  price      String
-  quantity   String
-  defense    String
-  evasion    String
-  multiplier String
-  info       String
+datasource db {
+  provider = "postgresql"
+  url = "***"
 }
-model RangeWeapon {
-  id         Int      @id @default(autoincrement())
+model MeleeWeapon {
+  id         Int      @default(autoincrement()) @id
   createdAt  DateTime @default(now())
-  img        String   @default("range")
+  img        String   @default("melee")
   name       String
   price      String
   quantity   String
   attack     String
@@ -36,12 +20,25 @@
   multiplier String
   info       String
 }
-model MeleeWeapon {
-  id         Int      @id @default(autoincrement())
+model Protector {
+  id        Int      @default(autoincrement()) @id
+  createdAt DateTime @default(now())
+  img       String   @default("protector")
+  name      String
+  price     String
+  quantity  String
+  defense   String
+  evasion   String
+  effect    String
+  info      String
+}
+
+model RangeWeapon {
+  id         Int      @default(autoincrement()) @id
   createdAt  DateTime @default(now())
-  img        String   @default("melee")
+  img        String   @default("range")
   name       String
   price      String
   quantity   String
   attack     String
```


